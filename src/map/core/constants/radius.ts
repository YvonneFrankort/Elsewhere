export const RADIUS_GEOAPIFY = 5;      // km (urban)
export const RADIUS_GEOJSON = 50;      // km (static nature)
export const RADIUS_OVERPASS = 20;     // km (fallback nature)
export const RADIUS_NPS = 50;          // km (national parks)
export const RADIUS_EVENTBRITE = 50;   // km (events)
export const RADIUS_OPENMETEO = 50;    // km (weather)

export const BORDER_BUFFER = 20;       // km (state border safety zone)
